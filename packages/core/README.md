# @sanqianx/ai-coding-event-bridge

Durable AI coding event capture bridge core for Claude Code, Codex and
OpenCode: journal with atomic `appendEvent()` / `appendCommitBoundary()`,
identity normalization without fake evidence, per-client connectors and
installers, consumer-cursor-bounded compaction, and a headless conversation
query. Node >= 18, CommonJS. See the repository README for the full contract.
